# 如何检查是否使用 Supabase 数据库

## 方法 1：查看页面顶部状态指示器

在页面顶部的统计信息区域，会显示一个"存储"状态：
- **"云端"**（绿色）= 正在使用 Supabase 数据库 ✅
- **"本地"**（黄色）= 使用 localStorage（Supabase 未配置）⚠️

## 方法 2：查看浏览器控制台

1. 打开浏览器开发者工具（按 `F12` 或 `Cmd+Option+I`）
2. 切换到 **Console（控制台）** 标签
3. 刷新页面
4. 查看日志信息：

### 如果使用 Supabase：
```
✅ 使用 Supabase 数据库
📊 Supabase URL: https://xxxxx.supabase.co
✅ Supabase 连接成功
```

### 如果使用 localStorage：
```
⚠️ Supabase 未配置，将使用 localStorage 作为降级方案
💾 数据将保存在浏览器本地存储
```

## 方法 3：查看网络请求

1. 打开浏览器开发者工具（`F12`）
2. 切换到 **Network（网络）** 标签
3. 刷新页面或执行操作（添加/编辑书籍）
4. 查找对 `supabase.co` 的请求：
   - 如果看到对 `xxxxx.supabase.co` 的请求 = 正在使用 Supabase ✅
   - 如果没有看到 Supabase 请求 = 使用 localStorage ⚠️

## 方法 4：检查 Supabase Dashboard

1. 登录 [Supabase Dashboard](https://app.supabase.com/)
2. 选择你的项目
3. 进入 **Table Editor** → **books** 表
4. 执行添加/编辑操作后，查看表中是否有新数据：
   - 有数据 = Supabase 正常工作 ✅
   - 无数据 = 可能在使用 localStorage 或配置有问题 ⚠️

## 方法 5：检查配置文件

打开 `config.js` 文件，检查：

```javascript
const SUPABASE_CONFIG = {
  url: "YOUR_SUPABASE_URL",  // 如果还是占位符，说明未配置
  anonKey: "YOUR_SUPABASE_ANON_KEY",  // 如果还是占位符，说明未配置
};
```

- 如果两个值都是占位符（`YOUR_SUPABASE_...`）= 未配置，使用 localStorage
- 如果已填入真实值 = 已配置，应该使用 Supabase

## 常见问题排查

### 显示"本地"但已配置 Supabase？

1. **检查 config.js**：确认 URL 和 Key 是否正确
2. **检查控制台错误**：查看是否有 Supabase 初始化错误
3. **检查 Supabase 库**：确认 CDN 链接是否正常加载
4. **检查网络**：确认能否访问 Supabase 服务

### 显示"云端"但数据未同步？

1. **检查 Supabase Dashboard**：确认表是否已创建
2. **检查 RLS 策略**：确认策略是否正确配置
3. **检查控制台错误**：查看是否有数据库操作错误
4. **检查网络请求**：查看 Network 标签中的错误响应

## 快速测试

在浏览器控制台执行：

```javascript
// 检查 Supabase 客户端
console.log("Supabase 客户端:", typeof supabase !== "undefined" ? "已加载" : "未加载");

// 检查配置
console.log("Supabase URL:", SUPABASE_CONFIG.url);
console.log("Supabase Key:", SUPABASE_CONFIG.anonKey ? "已配置" : "未配置");

// 测试连接（如果已配置）
if (typeof supabase !== "undefined" && supabase) {
  supabase.from('books').select('count').then(({data, error}) => {
    if (error) {
      console.error("❌ 连接失败:", error);
    } else {
      console.log("✅ 连接成功");
    }
  });
}
```
